<%= render partial: "shared/breadcrumbs" %>

<%= render partial: "hotels/header", locals: { hotel: @hotel } %>

<%= render partial: "hotels/gallery", locals: { photos: @hotel[:photos] } %>

<%= render partial: "hotels/description", locals: { hotel: @hotel } %>

<%= render partial: "hotels/rooms", locals: { rooms: @hotel[:rooms] } %>

<%= render partial: "hotels/reviews", locals: { reviews: @hotel[:reviews] } %>
